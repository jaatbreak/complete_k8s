# this command is used to create the docker registry secret this is the second type of the secret .
kubectl create secret docker-registry docker-secret --docker-email=example1@gmail.com --docker-username=amansingh32 --docker-password=321605 --docker-server=my-registry.example:500
then use the yaml file then :-
